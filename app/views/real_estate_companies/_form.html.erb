<div class="row">
  <div class="col-sm-3"></div>
  <div class="col-sm-6">
    <%= bootstrap_form_for(@real_estate_company, url: form_url,
      layout: :horizontal, label_col: "col-sm-4", control_col: "col-sm-8") do |form| %>
      <% if real_estate_company.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(real_estate_company.errors.count, "error") %> prohibited this real_estate_company from being saved:</h2>

          <ul>
          <% real_estate_company.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <%= form.text_field :name %>

      <%= form.text_field :website %>

      <%= form.text_field :address %>

      <%= form.select :founded, Time.zone.now.year.downto(Time.zone.now.year - 100) %>

      <%= form.number_field :revenue %>

      <%= form.text_field :synopsis %>

      <%= form.primary submit_label %>
    <% end %>
  </div>
</div>
